/*
let storeProducts = ['milk', 'cheese', 'cream', 'eggs']

let ed = prompt('Would you like to add a new product, yes, or delete a product, no,')
  if(ed == 'yes') {
    let newProd = prompt('Enter a new product')
    storeProducts.push(newProd) }
  else
    storeProducts.pop

 let message = ""
  for (i = 0; i+1)
      message = message + storeProduct[i] + "<br />"
      console.log(message)
*/
    
    